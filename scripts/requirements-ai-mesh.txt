# Install with: .venv311/bin/pip install -r scripts/requirements-ai-mesh.txt
#
# RTMW whole-body pose inference runs through MMPose.
# Use Python 3.12 for a clean install path. The current MMPose dependency
# chain still has trouble installing cleanly on Python 3.14 because of
# legacy transitive packages such as `chumpy` and `xtcocotools`.

torch>=2.12.0
mmpose>=1.3.0
mmengine>=0.4.0,<1.0.0
mmcv-lite>=2.0.0rc4,<2.2.0
mmdet>=3.0.0,<3.3.0
torchvision
opencv-python
numpy>=2.0.0
Pillow>=10.0.0
