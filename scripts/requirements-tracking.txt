# Dense point-tracking mesh generator (scripts/generate-mesh-tracking.py).
# Installs into the .venv (Python 3.11) torch env; runs on Apple-Silicon MPS.
#   PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/pip install -r scripts/requirements-tracking.txt
#
# CoTracker3 itself is fetched at runtime via torch.hub
# (facebookresearch/co-tracker, weights from huggingface.co/facebook/cotracker3),
# so it is not pinned here.
#
# BootsTAPIR (TRACKER=bootstapir|blend) runs the DeepMind torch port vendored
# under scripts/vendor/tapnet_torch. Its checkpoint is fetched once at runtime to
# ~/.cache/tapnet/bootstapir_checkpoint_v2.pt. The port needs dm-tree + einshape.
torch>=2.12.0
torchvision
numpy>=2.0.0
Pillow>=10.0.0
scipy
einops
imageio
imageio-ffmpeg
tqdm
dm-tree
einshape
transformers>=4.40.0
