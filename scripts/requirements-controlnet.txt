# Install with: pip install -r scripts/requirements-controlnet.txt
#
# On Apple Silicon (M1/M2) PyTorch uses Metal (MPS) — no CUDA needed.
# First-run model downloads: ~5.5GB total (SD1.5 ~4GB + ControlNet ~1.5GB)

torch==2.8.0
diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0
controlnet-aux>=0.0.7
Pillow>=10.0.0
